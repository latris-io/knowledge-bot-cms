module.exports = {
  // This will be triggered when files are deleted through the admin panel
  // We'll need to set up a different mechanism to track file deletions
  // since we can't extend the upload plugin without breaking permissions
}; 